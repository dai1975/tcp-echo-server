```
$ docker run --rm dai1975/tcp-echo-server -h

USAGE:
    tcp-echo-server [FLAGS] <PORT>

FLAGS:
        --datetime     return server's datetime
        --peer_addr    return peer address
    -h, --help         Prints help information
    -V, --version      Prints version information

ARGS:
    <PORT>    port number to bind
```


